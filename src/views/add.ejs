<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>新增內容</title>
  <link rel="stylesheet" href="/css/tailwind.css" />
</head>
<body class="bg-white px-4 py-12 flex flex-col items-center min-h-screen">
  <h1 class="text-3xl font-semibold text-gray-800 mb-6">➕ 新增中文語義資料</h1>

  <form method="POST" action="/add" class="w-full max-w-md space-y-4"
  onsubmit="this.querySelector('button').disabled = true; this.querySelector('button').classList.add('opacity-50');">
<input name="textChi" placeholder="輸入新增內容…" autofocus required
class="w-full border rounded px-4 py-2"  maxlength="512"/>
<button type="submit"
class="bg-green-500 text-white px-4 py-2 rounded hover:bg-green-600 transition">提交新增</button>
</form>

<% if (result) { %>
<p class="mt-4 text-sm text-<%= result.success ? 'green' : 'red' %>-600"><%= result.message %></p>
<% } %>
<a href="/" class="mt-8 text-blue-500 hover:underline">← 回首頁</a>

</body>
</html>