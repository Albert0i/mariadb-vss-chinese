<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>詳細內容</title>
  <link rel="stylesheet" href="/css/tailwind.css" />
</head>
<body class="bg-white px-4 py-12 flex flex-col items-center min-h-screen">
  <h1 class="text-3xl font-bold text-gray-700 mb-6">📄 詳細資料</h1>

  <div class="w-full max-w-xl bg-gray-50 shadow-md rounded p-6 text-left text-gray-700 text-base">
    <p>🆔 <%= record.id %></p>
    <p class="mt-2">💬 <strong>內容：</strong> <%= record.textChi %></p>
    <p class="mt-2">👁️‍🗨️ <strong>被查看次數：</strong> <%= record.visited %></p>
    <p class="mt-2">🕒 <strong>建立時間：</strong> <%= record.createdAt %></p>
    <p class="mt-2">🔄 <strong>更新時間：</strong> <%= record.updatedAt %></p>
    <p class="mt-2">🔖 <strong>更新代碼：</strong> <%= record.updateIdent %></p>
  </div>

  <div class="mt-10 flex gap-6 justify-center">
    <a href="javascript:history.back()" class="text-blue-500 hover:underline">← 回上一頁</a>
    <a href="/" class="text-blue-500 hover:underline">← 回首頁</a>
  </div>
</body>
</html>